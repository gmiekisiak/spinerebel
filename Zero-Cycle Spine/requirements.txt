# Pinned for reproducibility. Loosen only if you record the change.
numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.7,<4

# tkinter ships with CPython on Windows and macOS.
# Debian/Ubuntu: sudo apt install python3-tk
